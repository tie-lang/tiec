type tie<logic>
// r.1.1.3 扩展 2：全局 map 字面量初始化（键值形态）。
// 期望输出：3 / 2 / 1（len=3，m["b"]=2，m["a"]=1）
var m: map<i64> = ["a": 1, "b": 2, "c": 3]

func main() {
    println(to_string(len(m)))
    println(to_string(m["b"]))
    println(to_string(m["a"]))
}
