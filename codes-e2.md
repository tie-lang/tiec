# tie 诊断标号 E2xxx — 语法（Parser） / tie diagnostic codes E2xxx — 语法（Parser）

> 每种标号的成因与常见解决方案说明由 tie-diag 文档维护；本页为自动生成的目录骨架。

### E20001  ERR:? ?
- 消息：`ERR: <...>   <...>  `；消息名：ERR:? ?
- 出处：`/compiler/frontend/errors.tie`

### E20002  ?:
- 消息：`? <...> :
`；消息名：?:
- 出处：`/compiler/proto/parser.tie`

### E20003  extern 声明只能出现在文件顶层
- 消息：`extern 声明只能出现在文件顶层`；消息名：extern 声明只能出现在文件顶层
- 出处：`/compiler/frontend/errors.tie`

### E20004  不能在这里嵌套代码块
- 消息：`不能在这里嵌套代码块`；消息名：不能在这里嵌套代码块
- 出处：`/compiler/frontend/errors.tie`

### E20005  分号
- 消息：`分号`；消息名：分号
- 出处：`/compiler/frontend/putil.tie`

### E20006  变量 '?' 已移动（所有权已转移），不可再次使用（需要复制请用 clone）
- 消息：`变量 ' <...> ' 已移动（所有权已转移），不可再次使用（需要复制请用 clone）`；消息名：变量 '?' 已移动（所有权已转移），不可再次使用（需要复制请用 clone）
- 出处：`/compiler/frontend/errors.tie`

### E20007  块注释未闭合
- 消息：`块注释未闭合`；消息名：块注释未闭合
- 出处：`/compiler/frontend/errors.tie`

### E20008  字符串
- 消息：`字符串`；消息名：字符串
- 出处：`/compiler/frontend/putil.tie`

### E20009  字符串未闭合
- 消息：`字符串未闭合`；消息名：字符串未闭合
- 出处：`/compiler/frontend/errors.tie`

### E20010  字符字面量只能包含一个字符
- 消息：`字符字面量只能包含一个字符`；消息名：字符字面量只能包含一个字符
- 出处：`/compiler/frontend/errors.tie`

### E20011  字符字面量未闭合
- 消息：`字符字面量未闭合`；消息名：字符字面量未闭合
- 出处：`/compiler/frontend/errors.tie`

### E20012  整数
- 消息：`整数 `；消息名：整数
- 出处：`/compiler/frontend/putil.tie`

### E20013  文件结束
- 消息：`文件结束`；消息名：文件结束
- 出处：`/compiler/frontend/putil.tie`

### E20014  无法从 ? 初始化
- 消息：`无法从  <...>  初始化`；消息名：无法从 ? 初始化
- 出处：`/compiler/frontend/errors.tie`

### E20015  无法在 ? 后开始表达式
- 消息：`无法在  <...>  后开始表达式`；消息名：无法在 ? 后开始表达式
- 出处：`/compiler/frontend/errors.tie`

### E20016  无法识别的字符 '?'
- 消息：`无法识别的字符 ' <...> '`；消息名：无法识别的字符 '?'
- 出处：`/compiler/frontend/errors.tie`

### E20017  期望 ?，实际是
- 消息：`期望  <...> ，实际是 `；消息名：期望 ?，实际是
- 出处：`/compiler/frontend/errors.tie`

### E20018  期望标识符，实际是
- 消息：`期望标识符，实际是 `；消息名：期望标识符，实际是
- 出处：`/compiler/frontend/errors.tie`

### E20019  期望类型，实际是
- 消息：`期望类型，实际是 `；消息名：期望类型，实际是
- 出处：`/compiler/frontend/errors.tie`

### E20020  未知转义序列 \
- 消息：`未知转义序列 \`；消息名：未知转义序列 \
- 出处：`/compiler/frontend/errors.tie`

### E20021  标签 '?' 只能用于 while 或 for
- 消息：`标签 ' <...> ' 只能用于 while 或 for`；消息名：标签 '?' 只能用于 while 或 for
- 出处：`/compiler/frontend/errors.tie`

### E20022  标识符 '?'
- 消息：`标识符 ' <...> '`；消息名：标识符 '?'
- 出处：`/compiler/frontend/putil.tie`

### E20023  浮点数
- 消息：`浮点数 `；消息名：浮点数
- 出处：`/compiler/frontend/putil.tie`

### E20024  类型 '?'
- 消息：`类型 ' <...> '`；消息名：类型 '?'
- 出处：`/compiler/frontend/putil.tie`

### E20025  赋值目标必须是变量或字段
- 消息：`赋值目标必须是变量或字段`；消息名：赋值目标必须是变量或字段
- 出处：`/compiler/frontend/errors.tie`

### E20026  转义符后缺少字符
- 消息：`转义符后缺少字符`；消息名：转义符后缺少字符
- 出处：`/compiler/frontend/errors.tie`
