type tie<logic>
// r.1.1.5 扩展：局部 fn 表惰性 = [] 重赋值（修复同路径，验证不失一般性）。
func f() -> i64 {
    return 7
}

func main() {
    var u: table<fn() -> i64> = []
    table_push(u, f)
    println(to_string(u[0]()))
    u = []
    table_push(u, f)
    println(to_string(u[0]()))
}
