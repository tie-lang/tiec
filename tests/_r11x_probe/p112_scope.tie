type tie<logic>
// r.1.1.2 范围探测：char / f32 / 负浮点 / u 整型 / trit。
var gc2: char = 'A'
var gfc: f32 = 1.5
var gfn: f64 = -2.25
var gu64: u64 = 5

func main() {
    println(to_string(gc2))
    println(to_string(gfc))
    println(to_string(gfn))
    println(to_string(gu64))
}
