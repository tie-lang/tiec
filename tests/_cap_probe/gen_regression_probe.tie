type tie<logic>
// 临时回归：既有类型泛型在 tiec2（改动后）是否仍可用
func mx<T>(x: T) -> T {
    return x
}
func main() {
    var v: i64 = mx<i64>(5)
    var w = mx<string>("hi")
    println("v=" + to_string(v) + " w=" + w)
    if v != 5 { exit(1) }
    println("GENERIC REGRESSION PASS")
}