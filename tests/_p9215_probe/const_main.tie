// 引用方（默认 A1b 档）：
//   顶层 const / pub const 跨文件 ✓（顶层豁免）
//   ns pub const 跨文件 ✓（pub 导出）
//   ns 非 pub const 跨文件 ✗ → E00649（A2b：常量可见性跟随 L1 梯度）
import "./const_lib.tie"

func main() -> i64 {
    return TOP_A + TOP_PUB + thing::NS_PUB
}
