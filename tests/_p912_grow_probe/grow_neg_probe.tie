type tie<logic>
// grow_neg_probe.tie —— 负下标诊断 verify：t[i]=v 且 i<0 不得静默、不得越界写；
// 与下标读对齐 → 打印「运行时错误: 表下标越界（负索引）」并 exit(1)。
// 期望运行 rc=1 且输出含诊断消息（companion 探针，随 grow_probe.tie 验收）。
func main() -> i64 {
    var t = table_new_i64()
    t[1] = 11
    t[-3] = 999
    println("UNREACHED")
    return 0
}