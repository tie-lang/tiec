type tie<logic>
// 批次A 去桥探针：parse_trit 内联 IR 验证（-1/0/1；非法文本）
func main() {
    println(to_string(parse_trit("1")))   // 1
    println(to_string(parse_trit("0")))   // 0
    println(to_string(parse_trit("-1")))  // -1
}