/// Adds two numbers.
/// Second line.
func add(a: i64, b: i64) -> i64 {
    return a + b
}

/// A point struct.
struct Pt {
    /// x coord.
    var x: i64
    var y: i64
}

func main() {
    println(to_string(add(1, 2)))
}
