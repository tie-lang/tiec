type tie<logic>

// p.9.11.17 负例：函数类型不匹配 → 编译拒绝。
func add1(x: i64) -> i64 { return x + 1 }
func even(x: i64) -> bool { return x % 2 == 0 }

func main() {
    // even 返回 bool，期望 fn(i64)->i64 → 类型不匹配
    var g: fn(i64) -> i64 = even
    println(g(1))
}
