type tie<logic>

// T0.7/S10 负例：extern 声明带表参数 → 语义错误（extern 参数限标量/string/ptr/slice，
// 表/map/struct 等容器堆布局无法映射 C ABI 标量/指针参数）。
// 期望：`语义错误 @...: extern 函数 'foo' 的参数 't' 必须是标量/string/ptr/slice 类型，实际是 table<i64>`，编译失败 exit 1。
unsafe extern fn foo(t: table<i64>) -> i64;

func main() {
    println(1)
}
