type tie<logic>
// ============================================================
// p.9.11.14 import 别名负例：同一编译单元内两个 import 使用相同别名
// → 编译错误（error[E...]；回归把 *_neg*.tie 应拒编译视为 PASS）。
// ============================================================
import "./imp_alias_lib.tie" as dup
import "./imp_alias_lib2.tie" as dup

func main() {
    println("unreachable")
}
