type tie<class>
// p.9.11.14 import 别名/重导出正例辅助库 1（被 `as 别名` 导入）
namespace helpers {
    pub func greet() -> string {
        return "hello"
    }
    pub func add(a: i64, b: i64) -> i64 {
        return a + b
    }
}
