type tie<logic>

// ============================================================
// const 全局表负例验收（特性③）：
//   1. const 全局表重绑定赋值：`g = ...` → 语义错误
//      （const 只约束绑定，table_push 内容修改仍允许，见正例）
//
// 单文件编译时**第一个错误即停**（属预期）。
// 期望错误关键字：不能给 const 变量
// ============================================================

const g: table<i64>;

func main() {
    g = table_new_i64()
}
