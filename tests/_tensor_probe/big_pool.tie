type tie<logic>
func main() {
    var pool: array<f32, 262144> = array_init(0.0)
    var i: i64 = 0
    while i < 262144 {
        pool[i] = 1.0
        i = i + 1
    }
    println("pool[0]=" + to_string(as_f64(pool[0])) + " pool[262143]=" + to_string(as_f64(pool[262143])))
}