type tie<logic>
// 负例：常量越界读 / 负下标写 / 长度 0 / 元素类型错 / ref 字面量实参
func fill(z: ref array<f32, 8>) {
    z[0] = 1.0
}
func main() {
    var a: array<f32, 8> = array_init(0.0)
    var oob: f32 = a[99]
    println(to_string(oob))
}