type tie<class>
// tests/_p733_probe/calc_lib.tie —— p.7.3.3 字节码目标单函数样本（由 --target=trm 编译）
// 说明：class 角色单函数文件 → tieir 码仅 1 个函数 calc.add（出生序兼容子集，
// 满足 tieir_ser roundtrip 校验面）；由 --target=trm 产出 .tieir 供 trm 引擎
// 加载执行（闭环探针 trm_probe_733.tie）。
namespace calc {
    // 加法：19 + 23 = 42
    pub func add(a: i64, b: i64) -> i64 {
        return a + b
    }
}